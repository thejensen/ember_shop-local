
I'm the {{model.name}} object.

{{new-business saveBusiness="saveBusiness" category=model}}

<div class="row">
  <ul>
    {{#each model.businesses as |business|}}
      {{business-tile business=business}}
    {{/each}}
  </ul>
</div>
